"""Environment-specific Django settings for NexusCRM.

Select one with the ``DJANGO_SETTINGS_MODULE`` environment variable:

* ``config.settings.development`` - local development (default)
* ``config.settings.production``  - hardened production deployment
* ``config.settings.test``        - CI / pytest
"""
